#include <stdio.h>
int main(){
	int n;
	printf("nhap n:");
	scanf("%d",&n);
	if(n % 2 == 0 ) {
		printf("day la  so chan\n"); 
	} else  {
		printf("day la so le    \\n"); 
	}
	
	return 0; 
} 
